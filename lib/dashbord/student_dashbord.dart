import 'package:flutter/material.dart';

class StudentDashbord extends StatelessWidget {
  const StudentDashbord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Student Dashbord"),
      ),
    );
  }
}
